{
  "name": "clickflow",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"pnpm --dir server run dev\" \"npm run dev --prefix client\"",
    "start:server": "pnpm --dir server run dev",
    "start:client": "npm run dev --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.10"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1119.0",
    "kafkajs": "^2.2.4"
  }
}
