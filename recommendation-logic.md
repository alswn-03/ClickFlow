# 추천 로직 구성 및 확장 계획

### 1. 계획 중인 추천 로직

### 1.1 왜 단순 배치 재계산이 아닌가

가장 단순한 방식은 이벤트가 들어올 때마다 추천 전체를 재계산하는 것이다. 그러나 이 방식은 이벤트가 몰릴수록 연산량이 그대로 증가하여 병목이 발생하기 쉽다.

이를 피하기 위해 아래와 같은 구조를 채택하였다.

### 1.2 무거운 연산은 미리, 가벼운 연산만 요청 시점에

```
[Offline / 배치] 매일 새벽
  → S3에 쌓인 이벤트로 협업 필터링(ALS) 모델 재학습
  → 유저별 추천 후보(Top-N) 미리 계산
  → Redis에 저장

[Serving / 요청 시점]
  → 사용자 요청 발생
  → Redis에서 미리 계산된 추천 결과 조회
  → 즉시 반환
```

요청이 들어올 때마다 모델을 다시 돌리지 않는다. 무거운 연산(행렬 분해, 유사도 계산)은 배치 단계에서 이미 끝내두고, 서빙 단계에서는 Redis 조회만으로 응답한다.

### 1.3 이 구조를 선택한 이유

| 방식                         | 문제점                                      |
| ---------------------------- | ------------------------------------------- |
| 요청마다 실시간 계산         | 트래픽 증가 시 연산 비용도 함께 증가 → 병목 |
| 미리 계산 + 캐시 조회 (채택) | 요청 처리는 단순 조회이므로 안정적          |

Netflix, 쿠팡 등 실제 이커머스/스트리밍 서비스에서도 사용하는 패턴이다. 추천 자체는 대부분 배치로 미리 계산해두고, 실시간으로 필요한 부분(재고, 가격 등)만 온라인으로 처리한다.

### 1.4 반영 지연에 대한 인식

이 구조에서는 사용자의 방금 행동이 추천에 즉시 반영되지 않는다. 재학습 주기(매일 새벽)까지 기다려야 반영된다.

이는 의도된 트레이드오프다. 추천은 완벽하게 최신 상태일 필요가 없는 근사 문제에 가깝고, 약간의 지연은 사용자가 체감하기 어렵다고 판단하였다. 대신 재고/가격처럼 정합성이 중요한 데이터는 이 파이프라인과 무관하게 별도로 실시간 처리되어야 한다는 것을 인지하고 있다.

---

## 2. 준실시간 → 실시간 (반영 시간 단축)

지금 구조에서 반영 시간은 재학습 주기를 의미한다.

```
유저가 클릭
    ↓
Kafka → S3 (원본 데이터 쌓임)
    ↓
매일 새벽 배치 재학습 ← 여기서 반영됨
    ↓
Redis 갱신
    ↓
다음 요청부터 새 추천 반영
```

현재는 최대 24시간을 기다려야 반영된다.

**반영 시간 단축은 이 배치 주기를 줄이는 것을 의미한다.**

```
24시간 → 1시간 → 10분 → 1분...
```

다만 ALS 같은 협업 필터링 모델은 재학습 자체가 무겁고 오래 걸린다. 주기를 무작정 줄이면 서버 부하가 커지고, 재학습이 끝나기도 전에 다음 재학습이 시작되는 문제가 생길 수 있다.

진짜 단축 실험을 하려면 아래 두 가지 방법을 검토할 수 있다.

- 전체 데이터로 재학습(full recompute)하는 대신 증분 업데이트(incremental update)로 전환하는 방법
- 배치 주기 자체를 줄이면서 병목이 어디서 생기는지(모델 학습 시간 / 데이터 조회 시간 / Redis 쓰기 시간) 측정하고 개선하는 방법

둘 다 확정된 계획이 아니라, 2단계(여유 시) 실험에서 시도해볼 수 있는 접근 후보다. 어떤 방법을 실제로 적용할지는 그때 병목 위치를 확인한 뒤 결정한다.

### 2.1 진행 계획

```
1단계 (현재) : 준실시간 파이프라인 완성
  - 매일 새벽 배치로 모델 재학습
  - 재학습 전/후 성능 비교 후 수동 교체
  - Kafka 기반 파이프라인에서 순서 보장, 장애 복구 시나리오 검증

2단계 (여유 시) : 반영 시간 단축 실험
  - 배치 주기를 점진적으로 단축
  - 병목 지점 측정 및 개선
```

현재 1단계에서는 반영 시간 단축이 구현되어 있지 않다. 하루 1회 고정 배치이며, 2단계는 이 주기를 줄이면서 병목을 측정하고 개선하는 실험이다. 5개월 프로젝트 범위에는 포함하지 않으며, 여유가 있을 때 시도한다.

---

## 3. Feature Store로의 전환 (장기 계획)

현재 구조는 협업 필터링(ALS)으로 계산한 완성된 추천 결과를 Redis에 캐싱하는 방식이다.

Feature Store로 전환하면 유저/아이템의 피처 자체를 저장해두고, 모델(딥러닝, 세션 기반 등)이 그 피처를 바탕으로 추론하는 구조로 바뀐다.

### 3.1 Feature Store로 전환하고 싶은 이유

현재 구조는 협업 필터링(ALS)이 계산한 완성된 추천 결과를 Redis에 캐싱한다. 이 방식의 한계는, 유저의 방금 행동이 다음 배치 재학습 전까지는 추천에 전혀 반영되지 않는다는 점이다.

Feature Store로 전환하면 완성된 추천 결과 대신 유저/아이템의 피처를 저장해두고, 요청 시점에 최신 피처를 조합해 추론하는 구조로 바뀐다.

이렇게 하면 배치로 계산해둔 무거운 피처(장기 취향 등)와 방금 들어온 가벼운 피처(현재 세션의 클릭 흐름 등)를 함께 조합해 추천할 수 있어, "2단계: 반영 시간 단축"에서 목표로 한 실시간에 가까운 반영이 훨씬 자연스럽게 가능해진다.

즉 Feature Store 전환은 단순한 기술 교체가 아니라, 배치 재학습 주기까지 기다려야만 반영되는 현재 구조의 근본적인 한계를 해결하기 위한 다음 단계다.

다만 이 전환은 모델 자체도 실시간 피처를 입력받을 수 있는 구조(세션 기반, 딥러닝 등)로 함께 바뀌어야 의미가 있다. 협업 필터링(ALS)은 유저-아이템 전체 행렬을 배치로 분해하는 방식이라, Feature Store를 도입해도 요청 시점에 즉시 반영할 방법이 없기 때문이다.

#### 3.1.1 지금 도입하지 않는 이유

Model Registry도 같은 논리로 제외한 바 있다.

"Model Registry는 여러 모델 버전 관리와 A/B 테스트가 필요할 때 유용하지만, 이 프로젝트는 단일 모델 구조라 제외"

Feature Store 역시 지금은 오버엔지니어링이라고 판단하여 제외하였다. 모델이 협업 필터링 그대로인 상태에서 Feature Store만 도입하면, 저장 구조만 복잡해질 뿐 실질적인 반영 시간 개선은 없기 때문이다. 모델 교체와 Feature Store 도입은 함께 이루어져야 의미가 있다.

### 3.2 전환 타이밍

```
1단계 (현재)        : 협업 필터링 + Redis 캐싱
        ↓ (프로젝트 완료 후)
2단계 (여유 시)      : 반영 시간 단축 실험
        ↓ (모델 교체 시점)
3단계 (장기 계획)    : 세션 기반 등 모델 교체 → Feature Store 도입
```

Feature Store는 모델 자체를 교체하는 시점에 함께 검토한다. 지금 미리 도입하지 않는 이유는 오버엔지니어링을 경계했기 때문이며, 이후 모델 교체라는 명확한 계기가 생겼을 때 다시 판단한다.
