# 추천 로직 구성 및 확장 계획

## 1. 계획 중인 추천 로직

### 1.1 Consumer 구성과 역할

Kafka 도입 근거(S3와 추천 엔진, 두 Consumer가 동일 이벤트를 독립적으로 소비)에 따라 Kafka `user-events` 토픽을 구독하는 Consumer는 두 개다.

| Consumer           | 저장 대상                           | 역할                                                           |
| ------------------ | ----------------------------------- | -------------------------------------------------------------- |
| S3 Consumer        | S3 (Data Lake)                      | ML 재학습용 원본 이벤트 영구 저장                              |
| 추천 엔진 Consumer | Redis (`recent_activity:{user_id}`) | 유저의 최근 행동을 실시간으로 기록 (Speed Layer의 데이터 기반) |

두 Consumer는 각각 독립된 Consumer Group으로 동작하며, 한쪽이 지연되거나 장애가 발생해도 다른 쪽 소비에는 영향을 주지 않는다.

### 1.2 왜 "재학습 주기 단축" 대신 "Speed Layer 추가"인가

초기에는 반영 시간을 줄이는 방법으로 배치 재학습 주기 자체를 좁혀나가는 방식(24시간 → 1시간 → 10분 ...)을 고려했다.

그러나 실제 대규모 추천 시스템 사례를 조사한 결과, 이는 업계에서 흔히 쓰이는 방식이 아니었다. 대부분의 대규모 플랫폼은 배치 재학습 주기를 그대로 유지하면서(하루 1회 등), 그 위에 최근 유저 행동을 즉시 반영하는 별도의 실시간 레이어(Speed Layer)를 추가하는 Lambda Architecture를 사용한다. 무거운 배치 학습과 가벼운 실시간 반영을 처음부터 분리해두는 것이다.

이 방식을 선택한 이유는 다음과 같다.

- 재학습 자체를 자주 돌리는 것은 ALS 같은 협업 필터링 모델에는 비용이 크고, 재학습이 끝나기 전에 다음 재학습이 시작되는 문제가 생길 수 있다
- 반면 Speed Layer는 무거운 연산 없이 최근 행동만 가볍게 기록/반영하므로 부담이 적다
- 배치 레이어와 실시간 레이어를 분리하면 각각을 독립적으로 확장하거나 교체할 수 있다

### 1.3 배치 레이어 - 추천 로직 기본 구조

가장 단순한 방식은 이벤트가 들어올 때마다 추천 전체를 재계산하는 것이다. 그러나 이 방식은 이벤트가 몰릴수록 연산량이 그대로 증가하여 병목이 발생하기 쉽다.

이를 피하기 위해 아래와 같은 구조를 채택하였다.

```
[Offline / 배치] 매일 새벽
  → S3에 쌓인 이벤트로 협업 필터링(ALS) 모델 재학습
  → 유저별 추천 후보(Top-N) 미리 계산
  → Redis에 저장

[Serving / 요청 시점]
  → 사용자 요청 발생
  → Redis에서 미리 계산된 추천 결과 조회
  → 즉시 반환
```

요청이 들어올 때마다 모델을 다시 돌리지 않는다. 무거운 연산(행렬 분해, 유사도 계산)은 배치 단계에서 이미 끝내두고, 서빙 단계에서는 Redis 조회만으로 응답한다.

| 방식                         | 문제점                                      |
| ---------------------------- | ------------------------------------------- |
| 요청마다 실시간 계산         | 트래픽 증가 시 연산 비용도 함께 증가 → 병목 |
| 미리 계산 + 캐시 조회 (채택) | 요청 처리는 단순 조회이므로 안정적          |

Netflix, 쿠팡 등 실제 이커머스/스트리밍 서비스에서도 사용하는 패턴이다. 추천 자체는 대부분 배치로 미리 계산해두고, 실시간으로 필요한 부분(재고, 가격 등)만 온라인으로 처리한다.

이 구조에서는 사용자의 방금 행동이 추천에 즉시 반영되지 않는다. 재학습 주기(매일 새벽)까지 기다려야 배치 결과가 갱신된다. 이는 의도된 트레이드오프이며, 최근 행동을 즉시 반영하는 부분은 아래 2장의 Speed Layer가 보완한다.

---

## 2. 준실시간 → 실시간 (Speed Layer 추가)

### 2.1 추천 엔진 Consumer의 역할과 동작 방식

추천 엔진 Consumer는 Kafka `user-events` 토픽을 구독하여, 이벤트가 발생하는 즉시 해당 유저의 최근 활동을 Redis에 기록하는 역할을 한다.

```
Kafka (user-events)
    ↓
추천 엔진 Consumer (Consumer Group: recsys-consumer-group)
    ↓
Redis: recent_activity:{user_id} (예: 최근 클릭한 item_id 5개, 최신순)
```

이 Consumer는 S3 Consumer와 별도의 Consumer Group으로 동작하므로, S3 적재가 느려지거나 장애가 발생해도 추천 엔진 Consumer의 소비에는 영향을 주지 않는다. 이는 Kafka 도입 근거인 "두 Consumer의 독립적 소비"를 실제로 증명하는 구현이기도 하다.

1단계에서는 이 Consumer가 기록한 `recent_activity`를 실제 추천 결과 산출에 아직 반영하지 않는다. 우선 데이터를 쌓아두는 것까지만 구현하고, 이를 실제 재랭킹 로직에 활용하는 것은 2단계(여유 시)에서 진행한다.

### 2.2 Speed Layer란

Speed Layer는 Lambda Architecture에서 배치 레이어(Batch Layer)와 함께 동작하는 실시간 레이어를 말한다.

- **Batch Layer** : 무거운 연산(모델 재학습, 전체 추천 후보 계산)을 담당. 정확하지만 느리다 (반영까지 최대 24시간)
- **Speed Layer** : 가벼운 연산(최근 행동 기록, 경량 재랭킹)을 담당. 완벽하진 않지만 즉시 반영된다

두 레이어는 서로를 대체하지 않고 함께 동작한다. 최종 추천 결과는 Batch Layer가 만든 기본 추천(장기 취향 기반)에, Speed Layer가 제공하는 최근 행동 신호를 가볍게 얹어 보정하는 방식으로 결합될 예정이다 (2단계에서 구현).

### 2.3 Redis의 역할 변화

Redis는 1단계부터 두 가지 목적을 함께 가진다.

| Key 패턴                    | 생성 주체                        | 내용                               | 용도                 |
| --------------------------- | -------------------------------- | ---------------------------------- | -------------------- |
| `recommendation:{user_id}`  | 배치 재학습 (Batch Layer)        | 배치로 미리 계산된 Top-N 추천 결과 | 서빙 시 즉시 조회    |
| `recent_activity:{user_id}` | 추천 엔진 Consumer (Speed Layer) | 최근 클릭 등 실시간 행동 이력      | 향후 재랭킹의 입력값 |

1단계에서는 두 key가 독립적으로 존재하며, 서빙 로직은 여전히 `recommendation:{user_id}`만 조회한다. 2단계에서 재랭킹 로직이 추가되면, 서빙 시점에 두 key를 함께 조회해 결합하는 방식으로 확장한다.

### 2.4 진행 계획

```
1단계 (현재, 필수)
  - S3 Consumer: 배치 재학습용 원본 저장
  - 추천 엔진 Consumer: 최근 활동을 Redis(recent_activity)에 기록
  - 두 Consumer의 독립적 동작 확인 (Kafka 도입 근거 증명)
  - 서빙은 기존과 동일하게 recommendation:{user_id} 조회로만 처리

2단계 (여유 시)
  - recent_activity 데이터를 실제 재랭킹 로직에 반영
  - Inference Server가 recommendation + recent_activity를 조합해 응답
  - 반영 지연이 실제로 줄어드는지 측정
```

---

## 3. Feature Store로의 전환 (장기 계획)

현재 구조는 협업 필터링(ALS)으로 계산한 완성된 추천 결과를 Redis에 캐싱하는 방식이며, 여기에 Speed Layer(2장)가 최근 행동 원본을 별도로 가볍게 얹는다.

Feature Store로 전환하면 유저/아이템의 원시 행동이 아니라, 모델이 바로 소비할 수 있는 피처(임베딩, 집계 통계 등) 자체를 저장해두고, 요청 시점에 최신 피처를 조합해 추론하는 구조로 바뀐다.

### 3.1 Feature Store로 전환하고 싶은 이유

현재 구조(1단계 배치 + 2단계 Speed Layer)에서는 Redis에 완성된 추천 결과와 유저의 최근 활동 원본을 각각 저장해두고, 서빙 시점에 이 둘을 가볍게 결합하는 수준에 그친다. `recent_activity`는 최근 클릭 item_id 목록일 뿐, 모델이 바로 소비할 수 있는 피처 형태는 아니다.

Feature Store는 이 원시 행동 데이터를 모델이 바로 쓸 수 있는 피처로 가공해 저장하는 인프라로, 협업 필터링을 넘어 세션 기반 추천, 딥러닝 기반 모델 등 더 정교한 모델로 교체할 때 필요해진다.

즉 Feature Store 전환은 "반영 지연 문제 해결"을 위한 다음 단계라기보다, Speed Layer로 이미 확보한 실시간 반영 능력을 더 정교한 모델에서도 활용하기 위한 **모델 교체 시점의 인프라 확장**에 가깝다.

다만 이 전환은 모델 자체도 실시간 피처를 입력받을 수 있는 구조(세션 기반, 딥러닝 등)로 함께 바뀌어야 의미가 있다. 협업 필터링(ALS)은 유저-아이템 전체 행렬을 배치로 분해하는 방식이라, Feature Store를 도입해도 요청 시점에 즉시 반영할 방법이 없기 때문이다.

#### 3.1.1 지금 도입하지 않는 이유

Model Registry도 같은 논리로 제외한 바 있다.

> "Model Registry는 여러 모델 버전 관리와 A/B 테스트가 필요할 때 유용하지만, 이 프로젝트는 단일 모델 구조라 제외"

Feature Store 역시 지금은 오버엔지니어링이라고 판단하여 제외하였다. 현재 모델(ALS)과 Speed Layer(`recent_activity`)만으로도 1, 2단계 목표를 달성하는 데 충분하며, 모델이 그 피처를 소비할 수 있는 구조로 바뀌지 않는 한 Feature Store를 미리 도입해도 저장 구조만 복잡해질 뿐 실질적인 이득이 없다. 모델 교체와 Feature Store 도입은 함께 이루어져야 의미가 있다.

### 3.2 전환 타이밍

```
1단계 (현재)         : 협업 필터링(배치) + Speed Layer(Redis recent_activity)
        ↓ (2단계, 여유 시)
2단계                : Speed Layer 데이터를 재랭킹에 실제로 반영
        ↓ (모델 교체 시점)
3단계 (장기 계획)     : 세션 기반 등 모델 교체 → Feature Store 도입
```

Feature Store는 모델 자체를 교체하는 시점에 함께 검토한다. 지금 미리 도입하지 않는 이유는 오버엔지니어링을 경계했기 때문이며, 이후 모델 교체라는 명확한 계기가 생겼을 때 다시 판단한다.
